# 网格交易策略配置指南

## 📋 配置概述

网格交易策略包含丰富的配置选项，支持从开发测试到生产部署的完整生命周期管理。

## 🔧 基础配置

### 交易所设置
```yaml
exchange:
  binance:
    api_key: "your_binance_api_key"          # 币安API密钥
    secret_key: "your_binance_secret_key"    # 币安API密钥
    testnet: true                           # 是否使用测试网络
```

### 模拟模式设置
```yaml
grid_trading:
  simulation_mode: true  # 🔒 安全第一：默认启用模拟模式
```

## 🛡️ 安全配置

### 交易限制
```yaml
grid_trading:
  # 单笔订单最大金额，避免单笔损失过大
  max_single_order_amount: 100.0

  # 日交易总量限制，控制每日交易规模
  max_daily_trading_volume: 1000.0

  # 启用紧急停止功能
  emergency_stop_enabled: true
```

### 风险控制
```yaml
grid_trading:
  risk_limits:
    # 最大回撤限制（超过此值会停止交易）
    max_drawdown_percent: 20.0

    # 最大持仓比例（避免过度集中）
    max_position_size: 50.0

    # 订单金额范围控制
    min_order_amount: 10.0   # 最小10USDT
    max_order_amount: 500.0  # 最大500USDT
```

## 🔄 容错配置

### 重试机制
```yaml
grid_trading:
  # 订单提交失败时的重试次数
  order_retry_attempts: 3

  # 重试间隔（秒）
  order_retry_delay_seconds: 1
```

## 📊 监控配置

### 性能监控
```yaml
grid_trading:
  performance_monitoring:
    # 启用性能指标收集
    enable_metrics: true

    # 指标收集间隔（分钟）
    metrics_interval_minutes: 5

    # 胜率告警阈值（低于此值会告警）
    alert_win_rate_threshold: 0.4  # 40%
```

## 🚀 部署配置建议

### 开发环境
```yaml
# 开发测试阶段
grid_trading:
  simulation_mode: true
  max_single_order_amount: 10.0     # 小金额测试
  max_daily_trading_volume: 100.0   # 小规模测试
  risk_limits:
    max_drawdown_percent: 5.0       # 严格限制
```

### 测试环境
```yaml
# 功能验证阶段
grid_trading:
  simulation_mode: true             # 仍用模拟模式
  max_single_order_amount: 50.0     # 中等金额测试
  max_daily_trading_volume: 500.0   # 中等规模测试
  performance_monitoring:
    enable_metrics: true            # 启用详细监控
```

### 生产环境
```yaml
# ⚠️ 生产部署前务必充分测试！
grid_trading:
  simulation_mode: false            # 🔴 关闭模拟模式
  max_single_order_amount: 100.0    # 根据资金情况调整
  max_daily_trading_volume: 1000.0  # 根据策略调整
  emergency_stop_enabled: true      # 确保启用紧急停止
  risk_limits:
    max_drawdown_percent: 20.0      # 设置合理止损线
  performance_monitoring:
    enable_metrics: true            # 生产环境必须监控
```

## 🔍 配置检查清单

### 部署前检查
- [ ] `simulation_mode` 在生产环境设为 `false`
- [ ] `api_key` 和 `secret_key` 已正确配置
- [ ] `testnet` 在生产环境设为 `false`
- [ ] `emergency_stop_enabled` 已启用
- [ ] 风险限制参数符合资金情况
- [ ] 监控配置已启用

### 安全检查
- [ ] API密钥定期轮换
- [ ] IP白名单已配置
- [ ] 交易权限仅限于必要操作
- [ ] 监控告警已设置
- [ ] 紧急停止机制已测试

## ⚠️ 重要提醒

1. **安全第一**: 生产环境首次部署时，务必从小金额开始测试
2. **渐进式部署**: 建议按 开发→测试→生产 的顺序逐步放开限制
3. **监控到位**: 确保所有监控指标正常工作
4. **备份策略**: 保留完整的配置和密钥备份
5. **定期审查**: 定期检查和调整配置参数

## 📞 支持

如有配置问题，请检查：
1. 日志文件中的错误信息
2. 配置文件语法是否正确
3. API密钥是否有效
4. 网络连接是否正常